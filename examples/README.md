# Examples

These examples are safe, synthetic fixtures for local LSDF demos and integration checks. Sensitive-looking values use LSDF fixture tokens or reserved example domains so detectors can be exercised without publishing real secrets, PHI, customer data, or employee data.

Run examples through Docker Compose from the repository root.
